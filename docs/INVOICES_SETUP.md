# 💰 Модуль "Счета" (Invoices)

## ✅ Что реализовано

### Backend (FastAPI + SQLAlchemy)
- ✅ Модели `Invoice` и `InvoiceItem`
- ✅ 5 статусов счетов: Draft, Sent, Paid, Overdue, Cancelled
- ✅ Автогенерация номеров счетов (INV-YYYYMM-XXXX)
- ✅ Расчет сумм с НДС и скидками
- ✅ Связь с клиентами и проектами
- ✅ Автообновление статуса на Overdue
- ✅ API endpoints: GET, POST, PUT, DELETE, Mark as Paid

### Frontend (Next.js 15 + TypeScript)
- ✅ Страница списка счетов с фильтрацией
- ✅ Статистика по счетам (общая, оплачено, ожидается, просрочено)
- ✅ Форма создания счета
- ✅ Динамическое добавление/удаление позиций
- ✅ Авторасчет сумм
- ✅ Цветовые бейджи статусов
- ✅ Интеграция в главное меню

---

## 🚀 Запуск

### 1. Запусти Backend

```bash
cd backend

# Установи зависимости (если еще не установлены)
pip install -r requirements.txt

# Запусти сервер
uvicorn main:app --reload
```

Backend будет доступен на: `http://localhost:8000`

### 2. Запусти Frontend

```bash
# Из корневой папки
git pull
npm install
npm run dev
```

Frontend будет доступен на: `http://localhost:3000`

### 3. Открой в браузере

Перейди на: `http://localhost:3000/invoices`

---

## 📊 Функционал

### Список счетов
- Просмотр всех счетов
- Фильтрация по статусу
- Статистика по суммам

### Создание счета
1. Нажми **"Создать счет"**
2. Заполни основную информацию:
   - Название счета
   - Клиент (обязательно)
   - Проект (опционально)
   - Срок оплаты
   - НДС и скидка
3. Добавь позиции:
   - Название, описание
   - Количество, единица, цена
   - Добавляй неограниченное количество позиций
4. Укажи условия оплаты и примечания
5. Нажми **"Создать счет"**

### Авторасчет
Система автоматически рассчитывает:
- Промежуточную сумму (сумма всех позиций)
- Скидку в %
- НДС от суммы после скидки
- Итоговую сумму

---

## 📡 API Endpoints

### GET /invoices/
Получить все счета с фильтрацией

**Query параметры:**
- `status` - фильтр по статусу
- `client_id` - фильтр по клиенту
- `project_id` - фильтр по проекту

### POST /invoices/
Создать новый счет

**Body:**
```json
{
  "title": "Разработка сайта",
  "client_id": 1,
  "project_id": 2,
  "due_date": "2025-01-15T00:00:00",
  "tax_rate": 20,
  "discount": 10,
  "items": [
    {
      "name": "Дизайн",
      "quantity": 1,
      "unit": "шт",
      "price": 50000
    }
  ]
}
```

### PUT /invoices/{id}
Обновить счет

### DELETE /invoices/{id}
Удалить счет

### POST /invoices/{id}/mark-paid
Отметить счет как оплаченный

**Body:**
```json
{
  "payment_method": "Банковский перевод"
}
```

---

## 📝 Статусы счетов

| Статус | Описание | Цвет |
|---------|-----------|------|
| **Draft** | Черновик, еще не отправлен | Серый |
| **Sent** | Отправлен клиенту | Синий |
| **Paid** | Оплачен | Зеленый |
| **Overdue** | Просрочен (автообновление) | Красный |
| **Cancelled** | Отменен | Серый |

---

## 🛠️ Следующие шаги

### В разработке:
- [ ] Генерация PDF счетов
- [ ] Отправка счетов по Email
- [ ] Просмотр отдельного счета
- [ ] Редактирование счета
- [ ] Интеграция с платежными системами
- [ ] Рекурентные счета
- [ ] Шаблоны счетов

---

## 🐛 Известные проблемы

На данный момент нет.

---

Документация создана: 09.12.2025
